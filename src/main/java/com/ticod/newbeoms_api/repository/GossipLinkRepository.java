package com.ticod.newbeoms_api.repository;

import com.ticod.newbeoms_api.entity.GossipLink;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GossipLinkRepository extends JpaRepository<GossipLink, Long> {
}
